services:
  - type: web
    name: analizador-mineria
    env: python
    buildCommand: |
      pip install -r requirements.txt
    startCommand: gunicorn app_lite:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
